import QuoteSection from "@/components/common/QuoteSection";

export default function NightBegins() {
  return (
    <QuoteSection
      quote={`夜晚，

才是真正開始。`}
      subtitle={`當大部分的人已經休息，

花嶼的漁船才正準備出港。`}
    />
  );
}