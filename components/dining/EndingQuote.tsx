import QuoteSection from "@/components/common/QuoteSection";

export default function EndingQuote() {
  return (
    <QuoteSection
      quote={`Good ingredients

need

no exaggeration.`}
      subtitle={`真正的新鮮，

不用太多調味。`}
    />
  );
}